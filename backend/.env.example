# =============================================================================
# Totarr Environment Configuration
# =============================================================================
# Copy this file to .env and configure your settings
# IMPORTANT: Never commit .env files to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (Required)
# -----------------------------------------------------------------------------
# Choose ONE of the following database configurations:

# Option 1: SQLite (Simple, for development/testing)
# DATABASE_URL=sqlite:///./totarr.db

# Option 2: TimescaleDB/PostgreSQL (Recommended for production)
# Replace with your actual database credentials and host
DATABASE_URL=postgresql://totarr:YOUR_PASSWORD@YOUR_DATABASE_HOST:5432/totarr

# Examples:
# - Local PostgreSQL: postgresql://user:pass@localhost:5432/totarr
# - Unraid TimescaleDB: postgresql://totarr:mypass@192.168.1.100:5432/totarr
# - Docker network: postgresql://totarr:mypass@timescaledb:5432/totarr

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
ENVIRONMENT=production
PROJECT_NAME=Totarr

# -----------------------------------------------------------------------------
# Security (IMPORTANT: Change in production!)
# -----------------------------------------------------------------------------
# Generate a secure random string for SECRET_KEY
# You can use: openssl rand -hex 32
SECRET_KEY=change-this-to-a-random-secret-key-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# -----------------------------------------------------------------------------
# CORS Settings (For development with separate frontend)
# -----------------------------------------------------------------------------
# In production with Docker, you typically don't need to change this
# For development, add your frontend URL
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:8000"]

# -----------------------------------------------------------------------------
# Optional: Redis (For caching - not required yet)
# -----------------------------------------------------------------------------
# REDIS_URL=redis://redis:6379/0

# -----------------------------------------------------------------------------
# Optional: Celery (For background tasks - not required yet)
# -----------------------------------------------------------------------------
# CELERY_BROKER_URL=redis://redis:6379/0
# CELERY_RESULT_BACKEND=redis://redis:6379/0

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
# LOG_LEVEL=INFO

# =============================================================================
# Notes:
# =============================================================================
# - All integration configs (Plex, Radarr, Sonarr, etc.) are configured via
#   the web UI under Settings → Integrations
# - For Docker deployments, use service names (e.g., timescaledb, redis)
#   instead of localhost or IP addresses
# - For Unraid, use the Unraid server IP address for database host
# =============================================================================
